{
  "name": "lpi-github",
  "version": "1.0.0",
  "description": "LinchPin Github integration",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "github": "^0.2.4",
    "seneca": "^0.6.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.2",
    "mocha-lcov-reporter": "0.0.2",
    "istanbul": "^0.3.13",
    "chai": "2.3.0",
    "mocha": "*",
    "getenv":"*"
  }
}
